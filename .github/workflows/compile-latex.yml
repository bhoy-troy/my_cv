

name: Build LaTeX PDF

on:
  push:
    paths:
      - 'src/**/*.tex'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:



#      - name: Checkout Repository
#        uses: actions/checkout@v4
#        with:
#          repository: bhoy-troy/my_cv
#          ssh-key: ${{ secrets.GIT_SSH_PRIVATE_KEY }}

      - name: Checkout via SSH
        uses: actions/checkout@v4
        with:
            ssh-key: ${{ secrets.ACTIONS_DEPLOY_KEY }}




      - name: Compile LaTeX Document
        uses: dante-ev/latex-action@latest
        with:
          root_file: my_cv.tex
          working_directory: src


      - name: Save PDF as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my_cv.pdf
          path: my_cv.pdf



#      - name: Commit and push PDF
#        run: |
#          git config --global user.name "github-actions[bot]"
#          git config --global user.email "github-actions[bot]@users.noreply.github.com"
#          git add src/my_cv.pdf
#          git commit -m "Auto-build PDF from LaTeX" || echo "No changes to commit"
#          git push
#

      - name: Create Release

        uses: actions/checkout@v4
      - name: Run check of pwd
        run: pwd
      - name: Run check of what's in pwd
        run: ls

      - name: check for changes
        run: git status
      - name: stage changed files
        run: git add .
      - name: commit changed files
        run: git commit -m git commit -m "Auto-build PDF from LaTeX" || echo "No changes to commit"
#      - name: fetch from master
#        run: git fetch origin master
      - name: push changes
        run: git push

